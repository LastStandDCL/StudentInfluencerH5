import React from 'react';
import PageTitle from "../../components/PageTitle";

const CheckMaterialPage = () => {
    return (
        <div>
            <PageTitle text='宣讲材料审核'/>
        </div>
    );
};

export default CheckMaterialPage;
